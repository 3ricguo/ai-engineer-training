{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      // Or to the directory of the currently open file:
      "cwd": "${fileDirname}"
    }
  ]
}
